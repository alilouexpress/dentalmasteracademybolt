// Barrel module — re-exports the content layer (types, defaults, texts,
// API calls) so existing imports from '@/lib/content' keep working.
export * from './content/types';
export * from './content/defaults';
export * from './content/text';
export * from './content/api';
